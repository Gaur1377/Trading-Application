# Binance CLI Trading Application

## Description
This Python application allows you to place **Market** and **Limit** orders on **Binance Futures Testnet (USDT-M)**.  
It supports **BUY** and **SELL**, logs all API requests/responses, and validates user inputs.

## Setup Steps
1. Clone the repository or unzip the folder.
2. Create a virtual environment:
   ```bash
   python -m venv venv